from setuptools import setup, find_packages

setup(
    name="UR_Primary_Client_Python_Library",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "tabulate",
    ],
)